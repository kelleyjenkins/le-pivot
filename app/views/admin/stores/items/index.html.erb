


<div class="container">
    <div class="row">
        <div class="col-xs-12">
          <h1> Welcome admin for this store </h1>
           <%= link_to "Create New Item", new_admin_store_item_path, class: "btn btn-sm btn-info-outline",  style: 'float:right;' %>
          <p>Make edits to the items in this store below</p>
        </div>
    </div>
</div>


<div class="container">
    <div class="row">
        <div class="col-xs-12">
          <p></p>
        </div>
    </div>
</div>



<div class="container">
    <div class="row">
        <div class="col-xs-12">

          <div class="row">
            <% @items.each do |item| %>
              <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                  <%= image_tag item.image.url, size: "150" %>
                  <div class="caption">
                    <h6><%= link_to item.title, item_path(item) %></h6>
                    <p>Price: <%= number_to_currency(item.price) %></p>
                    <p><%= link_to "Edit", edit_admin_store_item_path(item.store.slug, item), class: "btn btn-sm btn-warning-outline"  %></p>
                    <% unless item.retired? %>
                      <%= link_to "Add to cart", carts_path(item_id: item.id), method: :post, class: "btn btn-sm btn-success-outline" %></center>
                    <% else %>
                      <p>Retired</p>
                    <% end %>
                  </div>
                </div>
              </div>
            <% end %>
          </div>

        </div>
    </div>
</div>























<% if false %>

<div class="container">
  <%= link_to "Create New Item", new_admin_store_item_path, class: "badge badge-success center" %>
  <div class="row">
    <% @items.each do |item| %>
      <div class="center-block">
        <div class="card border-white" style="width: 7rem;">
          <%= image_tag item.image.url(:medium), class: "card-img-top" %>
          <div class="card-body">
            <center><p class='small'><strong><%= link_to item.title, item_path(item) %></strong></p>
              <!-- <p>Description: <%= item.description %></p> -->
              <footer>
                <p class='small'>Price: $<%= item.price %></p>
                
                <p><%= link_to "Edit", edit_admin_store_item_path(item.store.slug, item), class: "badge badge-info"  %></p>
                <% unless item.retired? %>
                  <%= link_to "Add to cart", carts_path(item_id: item.id), method: :post, class: "badge badge-primary" %></center>
          <% else %>
            <div class="badge badge-secondary">
              Retired
            </div>
              </footer>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

</body>

<% end %>